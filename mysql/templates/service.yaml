apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: expense-1
spec:
  selector:
    component: mysql
    project: expense-1
    tier: db
  ports:
  - protocol: TCP
    port: 3306 #service-port
    targetPort: 3306 #target-port